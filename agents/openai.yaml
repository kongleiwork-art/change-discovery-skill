interface:
  display_name: "Change Discovery"
  short_description: "Classify, challenge value, and gate effort before code."
  default_prompt: "Use $change-discovery to classify this change, assess its preliminary product value, stop on decline or defer, recommend and confirm the model tier when proceeding or reducing scope, and clarify it before implementation."
